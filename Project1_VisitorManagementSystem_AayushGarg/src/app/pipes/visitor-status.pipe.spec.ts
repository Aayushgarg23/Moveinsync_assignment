import { VisitorStatusPipe } from './visitor-status.pipe';

describe('VisitorStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new VisitorStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
